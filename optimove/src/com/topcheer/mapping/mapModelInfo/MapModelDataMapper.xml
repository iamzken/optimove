<?xml version="1.0" encoding="GBK" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.topcheer.dao.mapModelInfo.MapModelDataMapper" >
    
  	<select id="getModelData" resultType="HashMap" parameterType="HashMap">
  		select * from ${tablename}
  	</select>
  	
  	<select id="getSuperModelData" resultType="HashMap" parameterType="HashMap">
  		${sql}
  	</select>
  	
  	<select id="searchRegionModelData" resultType="HashMap" parameterType="HashMap">
  		${searchsql}
  	</select>
</mapper>